# Production env - used when next build runs (e.g. on Render)
# Leave NEXT_PUBLIC_API_BASE empty so client uses proxy (/api/*) - avoids CORS for login
# NEXT_PUBLIC_API_BASE=
NEXT_PUBLIC_SITE_URL=https://realestate-frontend-h4u3.onrender.com
